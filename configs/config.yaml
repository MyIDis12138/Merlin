imports:
  - logging.yaml
  - model.yaml
  - training.yaml
  - dataset.yaml
  - runner.yaml

environment:
  seed: 42
  device: "cuda"

experiment:
  name: "breast_mri_classification"
  work_dir: "output/breast_mri_classification"
  tags:
    - "baseline"
    - "molecular_subtype"
  description: "Baseline model for breast MRI molecular subtype classification"
