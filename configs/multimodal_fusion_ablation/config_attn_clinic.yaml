imports:
  - logging.yaml
  - model_attn_clinic.yaml
  - training.yaml
  - dataset.yaml
  - runner.yaml

environment:
  seed: 42
  device: "cuda"

experiment:
  name: "multimodal_resnet3d_nphase_attn_clinic"
  work_dir: "work_dirs/experiments/multimodal_resnet3d_nphase_attn_clinic"
  tags:
    - "baseline"
    - "clinical_label"
  description: "Baseline model for breast MRI recurrance prediction"
